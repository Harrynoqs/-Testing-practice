class calculator {
    static add = (x, y) => {
      return x + y;
    }
    static subtract = (x, y) => {
      return x - y;
    }
    static multiply = (x, y) => {
      return x * y;
    }
    static divide = (x, y) => {
      return x / y;
    }
  }

  module.exports = calculator